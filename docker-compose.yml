services:
  dmarc-analyzer:
    build: .
    ports:
      - "3000:3000"
    environment:
  - ANTHROPIC_API_KEY=${ANTHROPIC_API_KEY}
    volumes:
      - dmarc-data:/data
    restart: unless-stopped

volumes:
  dmarc-data:
